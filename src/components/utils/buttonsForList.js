import React from 'react'
import Item from '../original_menu/original_menu_item'

export default function(props) {
    const delItems = props.deleteItems;
    const add = props.add;
    const select = props.select;

}